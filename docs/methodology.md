# Index Methodology

The project uses a Laspeyres-style price index.

The basic formulation is:

Index = Σ(weight_i × current_price_i / base_price_i) × 100

Weights represent the relative importance of routes, initially based on passenger traffic.

The index should be interpreted relative to a selected base period.
